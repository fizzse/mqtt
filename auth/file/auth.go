package file

type Auth struct {
}

func (a *Auth) Connect(username, password, clientId string) (err error) {
	return
}

func (a *Auth) Supper(username, password, clientId string) (err error) {
	return
}

func (a *Auth) Publish(username, password, clientId, topic string) (err error) {
	return
}

func (a *Auth) Subscribe(username, password, clientId, topic string) (err error) {
	return
}
